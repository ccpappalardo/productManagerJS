export default class userDTO{

    constructor(user){
        this.email=user.email;
        this.nombre=user.nombre;
        this.edad=user.edad;
        this.role=user.role;
        this.cart=user.cart;
    }
} 